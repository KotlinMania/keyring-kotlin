# Port targets — `keyring-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `keyring` that codex-rs uses, and therefore the minimum subset that `keyring-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-72] (rank `134`, tier `direct`, status `To Do`)

**Cargo package name**: `keyring`
**Rust module name (in `use` statements)**: `keyring`

## Summary

- **4** distinct symbols imported by codex-rs
- **8** total `use` statements
- **4** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `keyring::Error`

- **5 `use` site(s)** across **4 codex member crate(s)**: `codex-keyring-store`, `codex-login`, `codex-rmcp-client`, `codex-secrets`
- Aliases used: `as KeyringError`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `keyring-store/src/lib.rs:2` (`codex-keyring-store`) — `use keyring::Error as KeyringError`
- `keyring-store/src/lib.rs:112` (`codex-keyring-store`) — `use keyring::Error as KeyringError`
- `login/src/auth/storage_tests.rs:10` (`codex-login`) — `use keyring::Error as KeyringError`
- `rmcp-client/src/oauth.rs:598` (`codex-rmcp-client`) — `use keyring::Error as KeyringError`
- `secrets/src/local.rs:336` (`codex-secrets`) — `use keyring::Error as KeyringError`

### `keyring::Entry`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-keyring-store`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `keyring-store/src/lib.rs:1` (`codex-keyring-store`) — `use keyring::Entry`

### `keyring::credential::CredentialApi`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-keyring-store`
- Aliases used: `as _`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `keyring-store/src/lib.rs:113` (`codex-keyring-store`) — `use keyring::credential::CredentialApi as _`

### `keyring::mock::MockCredential`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-keyring-store`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `keyring-store/src/lib.rs:114` (`codex-keyring-store`) — `use keyring::mock::MockCredential`
