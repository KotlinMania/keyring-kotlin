# Port report — `keyring-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `keyring`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 8 `use` sites from 1 other repos
- **Outbound**: 27 `use` sites to 5 other repos
- **Unresolved external**: 6 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `keyring` | `keyring` | 8 | 5 | COD-72 | 134 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (8 edges, 4 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-keyring-store` | 5 | ✅ |
| `codex-kotlin` | `codex-login` | 1 | ✅ |
| `codex-kotlin` | `codex-rmcp-client` | 1 | ✅ |
| `codex-kotlin` | `codex-secrets` | 1 | ✅ |

### Symbol-level demand (4 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `keyring::Error` | 5 | 5 | 0 | 4 |
| `keyring::Entry` | 1 | 1 | 0 | 1 |
| `keyring::credential::CredentialApi` | 1 | 1 | 0 | 1 |
| `keyring::mock::MockCredential` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 4 symbols)

##### `keyring::Error` (5 sites, 5 codex, 0 other, 4 caller crates)

Codex call sites:

- `codex-kotlin/codex-keyring-store` `src/lib.rs:2` — `use keyring::Error as KeyringError`
- `codex-kotlin/codex-keyring-store` `src/lib.rs:112` — `use keyring::Error as KeyringError`
- `codex-kotlin/codex-login` `src/auth/storage_tests.rs:10` — `use keyring::Error as KeyringError`
- `codex-kotlin/codex-rmcp-client` `src/oauth.rs:598` — `use keyring::Error as KeyringError`
- `codex-kotlin/codex-secrets` `src/local.rs:336` — `use keyring::Error as KeyringError`

##### `keyring::Entry` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-keyring-store` `src/lib.rs:1` — `use keyring::Entry`

##### `keyring::credential::CredentialApi` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-keyring-store` `src/lib.rs:113` — `use keyring::credential::CredentialApi as _`

##### `keyring::mock::MockCredential` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-keyring-store` `src/lib.rs:114` — `use keyring::mock::MockCredential`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `log-kotlin` | 2 | 1 | `log` | COD-77 | 2 | To Do |
| `windows-sys-kotlin` | 20 | 20 | `windows-sys` | COD-192 | 4 | To Do |
| `zeroize-kotlin` | 1 | 1 | `zeroize` | COD-196 | 15 | To Do |
| `base64-kotlin` | 2 | 1 | `base64` | COD-24 | 22 | To Do |
| `clap-kotlin` | 2 | 2 | `clap` | COD-30 | 50 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `security-framework` | 11 | 10 | `keyring` |
| `secret-service` | 10 | 6 | `keyring` |
| `dbus-secret-service` | 9 | 5 | `keyring` |
| `byteorder` | 6 | 4 | `keyring` |
| `fastrand` | 4 | 1 | `keyring` |
| `linux-keyutils` | 3 | 3 | `keyring` |
